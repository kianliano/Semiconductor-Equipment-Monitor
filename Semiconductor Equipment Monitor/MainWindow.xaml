<Window x:Class="Semiconductor_Equipment_Monitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Semiconductor_Equipment_Monitor" 
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        d:DataContext="{d:DesignInstance Type=local:EquipmentViewModel}"
        mc:Ignorable="d"
        Title="半导体产线设备监测工具"  Height="600" Width="1000" Background="#39c5bb" Foreground="White">
    <Window.Resources>
        <local:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <!--
            <ObjectDataProvider x:Key="StatusEnum"
                            MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:EquipmentStatus"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>-->
    </Window.Resources>
    <Grid Margin="20">
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="Black" Offset="1"/>
                <GradientStop Color="#FF67FFAA" Offset="0"/>
                <GradientStop Color="#FF276241" Offset="0.919"/>
                <GradientStop Color="White" Offset="0.77"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <!--整体分为两行：顶部统计区+设备列表区-->
            <RowDefinition Height="Auto"/>
            <!--高度自适应-->
            <RowDefinition Height="*"/>
            <!--占满剩余空间-->
        </Grid.RowDefinitions>
        <!--第一部分 顶部统计区 包括 设备总数/运行状态/功能按钮-->
        <Grid Grid.Row="0" Margin="0 0 0 0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0 0 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!--数据统计-->
                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="{Binding TotalCount,StringFormat='设备总数：{0}'}" FontSize="16" FontWeight="Bold" Margin="10 10 20 0"/>
                    <TextBlock Text="{Binding RunningCount, StringFormat='运行中：{0}'}" FontSize="16" Foreground="#4CAF50" Margin="0 10 20 0"/>
                    <TextBlock Text="{Binding AlarmCount, StringFormat='报警：{0}'}" FontSize="16" Foreground="#F44336" Margin="0 10 20 0"/>
                    <TextBlock Text="{Binding OfflineCount,StringFormat='离线：{0}'}" FontSize="16" Foreground="#9E9E9E" Margin="0 10 20 0"/>
                    <TextBlock Text="{Binding IdleCount,StringFormat='空闲：{0}'}" FontSize="16" Foreground="#9E9E9E" Margin="0 10 20 0"/>
                </StackPanel>
                <!--操作按钮-->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="刷新" Width="80" Height="30" Background="#2196F3" Foreground="White" BorderThickness="0" Margin="0 10 10 0" Command="{Binding RefreshCmd}"/>
                    <Button Content="工单查询" Width="80" Height="30" Background="#2196F3" Foreground="White" BorderThickness="0" Margin="0 10 10 0" Command="{Binding QueryWorkOrderCmd}"/>
                    <Button Content="报警记录" Width="80" Height="30" Background="#2196F3" Foreground="White" BorderThickness="0" Margin="0 10 10 0" Command="{Binding AlarmRecordCmd}"/>
                    <Button Content="保存至MySQL" Width="100" Height="30" Background="#2196F3" Foreground="White" BorderThickness="0" Margin="0 10 10 0" Padding="8 2"  Command="{Binding Save2MySqlCmd}"/>
                    <Button Content="从MySQL读取" Width="100" Height="30" Background="#2196F3" Foreground="White" BorderThickness="0" Margin="0 10 5 0" Padding="8 2"  Command="{Binding LoadFromSqlCmd}"/>
                    <!--  <Button Content="" Width="10" Height="30" Background="#2196F3" Foreground="White" BorderThickness="0" Margin="0 10 5 0" Padding="8 2"  Command="{Binding UpdateStatusCmd}"/>-->
                </StackPanel>
            </Grid>
            <Grid Grid.Row="1" Margin="0 0 0 10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10 10 0 0">
                    <TextBox Text="{Binding FilterText,UpdateSourceTrigger=PropertyChanged}" 
                             Width="100" Margin="0 0 0 0">
                        <!-- 按回车搜索 -->
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCmd}"/>
                        </TextBox.InputBindings>
                        <!-- 占位符 -->
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                            <Style.Resources>
                                <VisualBrush x:Key="PlaceHolderBrush" Opacity="0.5" Stretch="None" AlignmentX="Left">
                                    <VisualBrush.Visual>
                                        <TextBlock Text="请输入搜索内容" Margin="5 0 0 0 "/>
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </Style.Resources>
                         
                            <Style.Triggers>
                                 <Trigger Property="Text" Value="">
                                     <Setter Property="Background" Value="{StaticResource PlaceHolderBrush}"/>
                                 </Trigger>
                                 <Trigger Property="IsFocused" Value="True">
                                     <Setter Property="Background" Value="White"/>  
                                 </Trigger>
                             </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <Button Content="清空" Width="100" Margin="10 0 285 0" Command="{Binding ClearFilterCmd}"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!--中间部分 设备列表-->
        <ListView Grid.Row="1" Background="#FFFCF5F5" BorderThickness="0" ItemsSource="{Binding FilterEqpView}">
            <ListView.View>
                <GridView>
                    <!-- <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/> -->
                    <GridViewColumn Header="设备编号" DisplayMemberBinding="{Binding EqpId}" Width="120"/>
                    <GridViewColumn Header="设备名称" DisplayMemberBinding="{Binding EqpName}" Width="120"/>
                    <GridViewColumn Header="状态"  Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                                    <!-- 状态指示灯 -->
                                    <Ellipse Width="10" Height="10" Fill="{Binding Status,Converter={StaticResource StatusToColorConverter}}" Margin=" 0 0 5 0"/>
                                    <!-- <TextBlock Text="{Binding Status}"/>-->
                                    <!-- 下拉框 -->
                                    <ComboBox Width="60"
                                              Margin=" 5 0 0 0"
                                              Loaded="ComboBox_Loaded"
                                              SelectedItem="{Binding Status,UpdateSourceTrigger=PropertyChanged}"
                                              SelectionChanged="ComboBox_Status_SelectionChanged">
                                    </ComboBox>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="创建时间" DisplayMemberBinding="{Binding CreateTime}" Width="150"/>
                    <GridViewColumn Header="产线编号" DisplayMemberBinding="{Binding LineId}" Width="100"/>
                    <GridViewColumn Header="今日产量" DisplayMemberBinding="{Binding OutputToday}" Width="120"/>
                    <GridViewColumn Header="最后更新时间" DisplayMemberBinding="{Binding LastUpdateTime}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
